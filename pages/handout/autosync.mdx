import Layout from "@templates/layout"

export default Layout
export const meta = { title: "Advanced improvements" }



## Automatic syncronization with Figma

What if we omit the step when a designer passes the design tokens
JSON file to a developer?

Try to setup [Figmagic](https://github.com/mikaelvesavuori/figmagic)
and pull the tokens automatically.